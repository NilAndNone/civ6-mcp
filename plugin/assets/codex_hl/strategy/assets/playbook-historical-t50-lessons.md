# Asset: strategy.playbook.historical_t50_lessons

## Purpose

This archived reference preserves non-executable lessons from earlier T50
experiments. It is not runtime policy and must not enter active strategy
snapshots.

## Scope

Allowed use:

- reference old run outcomes when explaining historical context;
- compare current live strict evidence with earlier evidence families;
- remind reviewers that early scouting, idle-turn avoidance, and
  evidence-backed settlement decisions were historically important.

Disallowed use:

- do not encode if/then action rules;
- do not call or recreate old runner logic;
- do not override live strict planner, gateway, or active strategy assets;
- do not import this archived asset into active prompt snapshots.
